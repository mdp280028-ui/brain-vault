# 2026-07-24

## What ran
research-opportunity was the workhorse today at 200 events, with watchdog close behind at 118, and ship-to-site at 115. notify ran 35 times, idea-agent 8, assignment-drafter 7, keyword-registry and domain-hunter 2 each, and single runs from scribe, run-batch, curator, cron, briefer, and backup-rsync. 3 events came in unattributed to an agent.

## What worked
Nothing completed cleanly enough to log — 0 tasks completed today despite heavy agent activity across the fleet.

## What didn't
153 errors logged, and they cluster hard around external fetches: youtube_fetch_failed (52) and stackexchange_fetch_failed (45) account for two-thirds of all failures, pointing at an upstream API or network issue rather than a logic bug. notify_failed (35) tracks almost exactly with notify's 35 runs — the notify agent appears to have failed on every single invocation today. watchdog_alert fired 9 times and claude_call_failed another 9. A handful of one-offs round it out: writer_fail_1, weekly_run_failed, assignment_draft_failed.

## Costs today
None logged — no model spend recorded for any tier today, which is inconsistent with 200+ research-opportunity runs and suggests either a logging gap or those runs failed before reaching a billable call.

## Surprises
notify failing 100% of the time (35/35) with zero completions across the whole fleet is the standout — combined with the fetch failures and the missing cost data, today looks less like "quiet day" and more like something upstream (network/API/auth) broke early and cascaded through agents that depend on it.

## Tomorrow
Root-cause the notify_failed streak and the youtube/stackexchange fetch failures before letting research-opportunity keep burning cycles against a broken pipeline — check credentials/rate limits first given the total lack of cost data alongside the volume of attempted calls.
