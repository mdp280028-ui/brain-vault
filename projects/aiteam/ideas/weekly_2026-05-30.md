# Weekly ideas — 2026-05-30

**Week:** `2026-W22`  
**Correlation:** `5FC25B0E-2357-488B-8F5D-96F1600F2744`  
**Ideas this week:** 6

---

## Verify and fix c_cost_per_call_delta_14d returning empty despite 16x cost spike

**Score:** 4.400000000000000356 · **Build:** 2.0h · **Impact:** `medium` · **Slug:** `diagnose-cost-per-call-delta-fix`

**Signal:** `diagnose.c_cost_per_call_delta_14d: [] + diagnose.l_token_cost_trajectory_30d (2026-05-14: $1.19 → 2026-05-24: $19.14, 16x in 10 days)`

The diagnose module includes a `c_cost_per_call_delta_14d` field designed to flag agents whose cost per call increased week-over-week. It returned `[]` on 2026-05-25, despite the fleet's daily cost rising from $1.19 to $19.14 over the prior 10 days — a 16x increase. The operator cannot currently determine whether this empty result means (a) the query requires a full 14-day window not yet available, (b) there is a logic error in the SQL, or (c) the cost spike was purely volume-driven with no per-call creep. All three explanations call for different responses.

Read `diagnose.sh` and locate the SQL behind `c_cost_per_call_delta_14d`. Run it manually against `~/store/aiteam.db` with `sqlite3` to see exactly what it returns and why. If the query requires a 14-day data window not yet present, add a sentinel row such as `{'status': 'insufficient_data', 'days_available': N}` instead of `[]`, so the operator sees the reason. If the query has a logic error (wrong JOIN, division-by-zero guard silently dropping rows, missing HAVING threshold), fix it. If the query is structurally correct and the cost spike was purely from increased call volume, add a comment to the schema confirming that, so a future operator doesn't re-investigate.

Track B: Failure mode — an agent's per-call cost silently doubles (e.g., curator_cowen's context grows as its topic list expands), pushing the daily MA from its current $5.77 toward the $25 soft cap with no warning. The cap fires, the entire fleet halts, and the operator debugs blind. Frequency: the 16x rise in 10 days demonstrates this pattern is live; recurrence is not hypothetical. Impact: fleet halt is approximately 4 hours recovery per incident — identify the culprit agent, fix prompt/context, restart all crons — plus any drafts or curator cycles missed during the outage window.

If the investigation reveals the query is correct and the field is empty because the spike was purely volume-driven, the downside is 1-2 hours of build time with no net change to observability.

---

## Add PID liveness check to lockfile logic to prevent permanent silent agent stoppage

**Score:** 4.0 · **Build:** 2.0h · **Impact:** `medium` · **Slug:** `stale-lockfile-pid-guard`

**Signal:** `diagnose.e_repeated_failures_14d[2] (lock_skip: 6, example payload {"existing_pid": 69401}) + search result unix.stackexchange.com/questions/12815 (PID lock file validation idiom)`

The `e_repeated_failures_14d` table shows 6 `lock_skip` events in 14 days with payload `{'existing_pid': 69401}`. The current lockfile pattern writes a PID on entry and skips all subsequent invocations if the file exists. It does not verify whether the recorded PID is still alive. When a cron-invoked agent crashes mid-run (OOM kill, timeout, SIGKILL from the OS), the lockfile remains with a now-dead PID. Every subsequent cron invocation silently skips. The agent produces no output and writes no failure entry to audit_log. No Telegram alert fires. The failure is invisible until the operator notices the silence.

Audit all lockfile acquire paths in `~/agents/lib/` and key agent entry scripts. For each path, add a PID liveness check with `kill -0 $pid 2>/dev/null`: if the stored PID is not running, remove the stale lockfile with `rm -f` and proceed normally. The `unix.stackexchange.com/questions/12815` search result confirms `kill -0` as the POSIX-standard idiom for this check. The fix is a two-line addition per lock-acquire site.

Track B: Failure mode — agent crashes mid-run, leaving a stale lockfile that permanently blocks all subsequent cron invocations. The agent silently stops drafting, curating, or checking watchdog state. Watchdog may not detect this condition if the agent's heartbeat is written at start rather than end. Frequency: 6 lock_skip events in 14 days; how many are stale vs. legitimate concurrent runs is currently unknown — the payload shows the same PID 69401 referenced but does not disambiguate. Impact: undetected agent stoppage for potentially days; recovery requires the operator to manually find and delete the stale lockfile, approximately 0.5-1 hour per incident depending on which agent is affected.

The PID recycling race is the principal downside: if the OS reuses the dead agent's PID for an unrelated process in the window between the liveness check and the lockfile removal, `kill -0` will return success against the wrong process and the stale lockfile will be incorrectly retained. This scenario is negligible on a single-user Mac Mini with low process churn but is real in theory.

---

## Tombstone config-synth slugs after 3 consecutive empty-output failures

**Score:** 4.0 · **Build:** 2.5h · **Impact:** `medium` · **Slug:** `config-synth-stuck-slug-tombstone`

**Signal:** `diagnose.e_repeated_failures_14d[3]: config_synth_failed n=5, example_payload slug=asbestos-testing-near-me, reason='[synthesize] FATAL: ai-do.sh produced empty output', gate_failed=sonnet`

The config synthesis step has failed 5 times in 14 days for asbestos-testing-near-me with reason 'ai-do.sh produced empty output'. No failure ceiling exists: the slug re-enters the synthesis queue each run and fires another Sonnet call indefinitely. Each wasted call costs $0.05–0.15 with zero output.

Add a `config_synth_failures` table to aiteam.db: (slug TEXT PRIMARY KEY, failure_count INT DEFAULT 0, last_failed_at TEXT, tombstoned INT DEFAULT 0). On each config_synth_failed event, increment failure_count via INSERT OR REPLACE. Before calling ai-do.sh for a slug, check tombstoned=1 and skip. When failure_count reaches 3, set tombstoned=1 and log a config_synth_tombstoned audit event with slug and failure history. The diagnose phase queries tombstoned slugs and includes them in the operator digest; un-tombstone is a direct DB edit.

Track B: specific failure mode — slug retried indefinitely on a permanent model refusal or bad prompt with no ceiling; observed frequency — 5 failures in 14 days for the same slug, ~2.5/week at current rate; impact if uncapped — $0.30–0.90/week per stuck slug in wasted Sonnet calls, plus inflated failure logs that suppress new genuine incidents in the diagnose output.

If a slug tombstones due to a transient API outage rather than a permanent failure condition, the operator must manually reset failure_count in the DB to retry — this adds a low-frequency manual intervention path that does not exist today.

---

## Diagnose and fix analyst_casper 372% cost-per-call spike

**Score:** 4.0 · **Build:** 2.0h · **Impact:** `medium` · **Slug:** `analyst-casper-cpc-spike`

**Signal:** `c_cost_per_call_delta_14d[0] (analyst_casper: +372%, $0.033 → $0.156/call) + c_cost_per_call_delta_14d[1] (curator_cowen: +128%, $0.020 → $0.045/call)`

analyst_casper's cost per call jumped 372% over 14 days, from $0.033 to $0.156. At 36 calls in 7 days (~20/week), weekly cost is ~$3.12 vs an expected ~$0.66 — excess of ~$10.40/month. curator_cowen shows an adjacent 128% CPC increase ($0.020 → $0.045) at 112 calls/7 days (~$11.20/month excess). Both can be investigated in a single session. A parallel investigation of asbestos-auditor is already pending (proposal: asbestos-auditor-cost-root-cause); this idea covers the two agents where the c_cost_per_call_delta_14d data is now showing the largest measured spikes.

Root cause candidates: prompt length grew (longer taxonomy or context injected per call), response length increased (more verbose JSON output), a second LLM call was added in a recent commit, or model routing changed. The audit_log captures cost_usd per call; grouping casper's and curator's calls by calendar day and comparing average cost to git log on that date will isolate when each spike started and which commit caused it.

The first step is a read-only audit under 2 hours: query audit_log for casper and curator call costs grouped by day over 14 days, identify the inflection date, then run git log --since on that date to find the causative commit. The fix follows directly from the root cause — most likely a prompt trim or a response-length constraint added to the agent's system prompt.

If the cost increase reflects intentional deeper analysis (e.g., longer context window added for better signal quality), reverting it trades analysis quality for ~$21/month in savings. The operator decides whether the quality delta is worth the cost.

---

## Add 403-streak backoff to Reddit poll source

**Score:** 4.0 · **Build:** 3.0h · **Impact:** `medium` · **Slug:** `reddit-403-persistent-backoff`

**Signal:** `e_repeated_failures_14d[1] (reddit_fetch_failed: n=33, http_status=403) + e_repeated_failures_14d[2] (poll_source_failed: n=31, source=reddit)`

Reddit has returned HTTP 403 on every fetch attempt for at least 14 days: 33 reddit_fetch_failed events and 31 poll_source_failed events are logged, generating ~4.6 identical failure entries per day. No backoff logic exists — the poll agent retries on the same schedule regardless of consecutive-failure count. The result is a sustained stream of identical audit_log entries that desensitizes the operator to failure alerts and burns poll cycles on a provably-blocked source.

The fix adds a 403-streak counter to the Reddit poll source. After 5 consecutive 403s with no intervening success, the agent sets a 24-hour skip flag for the Reddit source and sends one Telegram alert ('Reddit source suspended 24h — 5 consecutive 403s, manual action required'). The skip is cleared automatically the next day and the streak counter resets. This converts a chronic silent flood into a one-time actionable alert and stops accumulating ~64 noise entries per week in audit_log.

Failure mode prevented: audit_log flooded with identical 403 entries eroding the operator's ability to detect real failures. Frequency: ongoing — 33 fetch failures in 14 days with no sign of abating, ~2.4 failure events per day. Impact if not fixed: signal-to-noise in audit_log degrades indefinitely; whichever real failure eventually appears next to a wall of Reddit 403s is at elevated risk of being overlooked.

Downside: a threshold miscalibrated too low could suppress Reddit polling during a temporary rate-limit (e.g., 30-minute IP block) if it fires 5 times before clearing; the operator can manually delete the skip flag to recover early.

---

## Fix agent_caller=unknown in timeout_kill audit events

**Score:** 4.0 · **Build:** 2.0h · **Impact:** `medium` · **Slug:** `timeout-kill-agent-attribution`

**Signal:** `e_repeated_failures_14d[5] (timeout_kill: n=5, agent_caller='unknown', command_excerpt confirms claude -p invocation)`

Five timeout_kill events fired in 14 days, each with agent_caller='unknown' in the payload. Without attribution, every timeout_kill is unactionable: the operator cannot determine which agent is consistently timing out, whether it is one repeat offender or five different agents, or whether 1800s is an appropriate ceiling for that agent's actual workload. At ~10 kills/month, the system is discarding 30-minute compute sessions with no path to diagnosis or fix.

The payload confirms the failure mode: command_excerpt shows 'claude -p --model sonnet --max-turns 30 (prompt 13669 chars)' but agent_caller is 'unknown'. The calling script is not setting AGENT_CALLER (or the equivalent env var) before launching claude -p. The fix is a grep sweep of all scripts in ~/agents that invoke claude -p — each missing site gets AGENT_CALLER set from its own filename or a hardcoded name. The timeout handler already reads this variable; no handler changes are needed.

Failure mode prevented: timeout kills remain permanently unattributable and unresolvable. Frequency: 5 in 14 days (~10-11/month, every 1.3 days). Impact if not fixed: every future timeout_kill produces an unactionable audit_log entry; the repeat-offending agent (if one exists) continues to consume 1800s per kill with zero operator leverage to intervene.

Downside: if agent_caller is already being set upstream and the 'unknown' is a parsing bug inside the timeout handler itself rather than a missing env var at the call sites, the grep-and-patch approach fixes the wrong layer and attribution remains broken.

---


## Trend

- Last 4 weeks: 13 proposed, 0 built (rate: 0)
- Prior 4 weeks: 0 proposed, 0 built (rate: -)
- Trend vs prior: **not enough data**


## DEFERRED.md parse failures (operator triage needed)

12 items in DEFERRED.md cannot be auto-aged. Add a `Status: ... YYYY-MM-DD` line or move to closed:

- **D043** [section] — `mentioned_assets` multi-asset convergence
- **D055** [section] — Site repo node_modules disappeared
- **D075** [section] — Opportunity Scout Agent (business-idea discovery, separate from Agent A)
- **D059** [table] — SSG deploy throttle (pattern-copy of asbestos throttle at commit 3ed57dc)
- **D-SSG-01** [table] — 11 remaining asbestos path references in `run-batch.sh` (all in COMMENT lines)
- **D-SSG-02** [table] — Operator review of `SSG_TEMPLATES.md` Cosmetic Tolerance List + Verdict Rules
- **D-SSG-03** [table] — Confirm `relatedLinks` slug shape in SSG content
- **D-SSG-04** [table] — Reconcile AS1-AS5 anti-sameness checks between `SSG_TEMPLATES.md` and `audit_guide.py`
- **D-SSG-06** [table] — `AUDIT_SPEC.md` SPC-1..14 (~160 lines) still has UST/asbestos vocabulary
- **D-SSG-07** [table] — Stale "2,000-2,500 words" echo on `run-batch.sh` line ~357
- **D-SSG-08** [table] — `ctx.sh` has 22 asbestos references
- **D-SSG-09** [table] — `audit_guide.py` has 5 asbestos references in docstring/comments

