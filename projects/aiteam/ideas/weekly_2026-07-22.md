# Weekly ideas — 2026-07-22

**Week:** `2026-W30`  
**Correlation:** `852927DC-9FE3-4BAE-AF59-AF931FEA29D9`  
**Ideas this week:** 1

---

## Alert operator when stackexchange_fetch_failed exceeds 100/week (already at 114)

**Score:** 3.600000000000000088 · **Build:** 1.0h · **Impact:** `medium` · **Slug:** `stackexchange-fetch-failure-alert`

**Signal:** `diagnose.e_repeated_failures_14d[4] (stackexchange_fetch_failed, n=114)`

diagnose.e_repeated_failures_14d[4] shows stackexchange_fetch_failed at 114 occurrences with http_status "000000" (a malformed/impossible status, not a real 4xx/5xx from the server). This is distinct from the two existing pending proposals (stackexchange-fetch-null-status-fix and stackexchange-null-status-circuit-breaker) which target the underlying cause and a per-tag breaker. This idea is narrower: a threshold trip-wire in the existing daily digest that fires when weekly stackexchange_fetch_failed count crosses 100, so the operator is notified even if the two upstream fixes are not yet built or regress silently after being built.

The fix and breaker ideas already pending cover root-cause and mitigation. This idea covers detection-of-regression: a one-line SQL check added to the existing daily digest script (no new agent, no new schedule) that counts stackexchange_fetch_failed in audit_log over the last 7 days and appends a warning line if over 100. This is cheap insurance that the two pending ideas, once built, don't silently stop working without anyone noticing.

Downside: this could fail because it adds one more line item to a digest the operator may already be trimming for noise; if the operator considers 100/week an arbitrary threshold, they will just ignore the line, wasting the code path. Frequency: currently active every week (114 in the current window alone) until the underlying fix ships, so this is not speculative — it fires immediately.

---


## Trend

- Last 4 weeks: 23 proposed, 0 built (rate: 0)
- Prior 4 weeks: 18 proposed, 0 built (rate: 0)
- Trend vs prior: **flat**


## DEFERRED.md parse failures (operator triage needed)

12 items in DEFERRED.md cannot be auto-aged. Add a `Status: ... YYYY-MM-DD` line or move to closed:

- **D043** [section] — `mentioned_assets` multi-asset convergence
- **D055** [section] — Site repo node_modules disappeared
- **D075** [section] — Opportunity Scout Agent (business-idea discovery, separate from Agent A)
- **D059** [table] — SSG deploy throttle (pattern-copy of asbestos throttle at commit 3ed57dc)
- **D-SSG-01** [table] — 11 remaining asbestos path references in `run-batch.sh` (all in COMMENT lines)
- **D-SSG-02** [table] — Operator review of `SSG_TEMPLATES.md` Cosmetic Tolerance List + Verdict Rules
- **D-SSG-03** [table] — Confirm `relatedLinks` slug shape in SSG content
- **D-SSG-04** [table] — Reconcile AS1-AS5 anti-sameness checks between `SSG_TEMPLATES.md` and `audit_guide.py`
- **D-SSG-06** [table] — `AUDIT_SPEC.md` SPC-1..14 (~160 lines) still has UST/asbestos vocabulary
- **D-SSG-07** [table] — Stale "2,000-2,500 words" echo on `run-batch.sh` line ~357
- **D-SSG-08** [table] — `ctx.sh` has 22 asbestos references
- **D-SSG-09** [table] — `audit_guide.py` has 5 asbestos references in docstring/comments

