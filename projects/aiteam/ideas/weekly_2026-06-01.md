# Weekly ideas — 2026-06-01

**Week:** `2026-W23`  
**Correlation:** `562ABB5C-C23C-4D3A-BDBD-3DE6BB3A48C2`  
**Ideas this week:** 2

---

## Tombstone assignment slugs after 3 consecutive validation failures

**Score:** 3.799999999999999823 · **Build:** 3.0h · **Impact:** `medium` · **Slug:** `assignment-draft-validation-tombstone`

**Signal:** `diagnose.e_repeated_failures_14d[4] (assignment_draft_failed, n=10, reason=validation, site=asbestos, slug=3m-respirator)`

The assignment drafter logged 10 validation failures in 14 days (diagnose.e_repeated_failures_14d[4], reason='validation', site='asbestos'). The example slug — 3m-respirator — also appears in config_synth_failed and skipped events with reason='synth_failed', indicating the same slug is cycling through multiple pipeline stages and failing repeatedly. No ceiling on retries exists: a slug that fails validation re-enters the queue on the next drafter tick, consuming queue capacity and (if validation fires post-Sonnet) LLM budget each cycle.

The fix is a consecutive-failure counter on the slug queue — either a dedicated column or a lightweight tombstone table — that increments on each validation failure and halts re-queuing after 3 consecutive failures, writing a `slug_tombstoned` audit event for operator review. The config-synth-stuck-slug-tombstone idea (pending) implements this pattern for config synthesis; this applies it one phase earlier at assignment draft validation, covering a different failure mode in a different script.

The failure mode prevented is an indefinitely-cycling slug that occupies a queue slot and generates orphaned drafter_tick_started rows without ever producing a shippable article. At 5 failures per week, the throughput tax is ongoing and directly competes with the $200/month revenue target: at audit_guide's current 33% pass rate (diagnose.g_verdict_pass_rate_14d), every unshippable slug cycling in the queue displaces a slug that could pass. Estimated frequency: 5 validation failures per week currently. Estimated impact if unaddressed: queue partially occupied by structurally unshippable slugs for the lifetime of each stuck slug, constraining the content pipeline that is the primary path to revenue.

If this is wrong: validation failures might be transient — slug metadata temporarily missing, dependency not yet resolved — in which case a 3-failure tombstone permanently suppresses a slug that would eventually pass. Mitigation: make the threshold configurable and emit the tombstone event so the operator can manually clear it.

---

## Add EXIT traps to loop, domain-hunter, and rebuild-tick cron scripts

**Score:** 3.600000000000000088 · **Build:** 2.0h · **Impact:** `medium` · **Slug:** `non-drafter-exit-trap-sweep`

**Signal:** `diagnose.h_orphaned_starts_7d (loop_started: 5, dh_run_start: 4, rebuild_tick_started: 3)`

Three cron-triggered scripts logged orphaned starts in the past 7 days with no paired completion events: loop_started (5), dh_run_start (4), and rebuild_tick_started (3), totalling 12 orphaned rows per week in audit_log (diagnose.h_orphaned_starts_7d). The drafter has the identical structural problem at 205 orphans/week, and drafter-tick-completion-trap is pending to fix it using an EXIT trap pattern. That fix does not cover these three components.

Adding `trap 'log_to_audit ... completed/failed' EXIT` to each of the three scripts — using the same battle-tested pattern from the pending drafter fix — closes the gap. Each script already has an entry log call; the trap goes immediately after. Estimated 30 minutes per script.

The failure mode prevented is progressive audit_log signal degradation. The diagnose module's h_orphaned_starts_7d section exists specifically to surface this anomaly. At 12 orphaned rows per week across three components, the orphan count compounds monthly. As the non-drafter agents run more frequently (domain-hunter recently added a 4th site, loop cadence is daily), this count will grow. Diagnose signal quality is the mechanism by which this agent surfaces real problems; noise in that signal reduces its reliability. Estimated frequency: 12/week currently, growing. Estimated impact per week of inaction: one additional orphan row per component per day, permanently degrading the orphan baseline used to spot new regressions.

If this is wrong: EXIT traps fire on every exit path including clean exits. If a script exits with an unexpected non-zero code that the trap misclassifies, the trap could suppress a real failure signal. Risk is low given the pattern is already proven in the drafter context.

---


## Trend

- Last 4 weeks: 19 proposed, 0 built (rate: 0)
- Prior 4 weeks: 5 proposed, 0 built (rate: 0)
- Trend vs prior: **flat**


## DEFERRED.md parse failures (operator triage needed)

12 items in DEFERRED.md cannot be auto-aged. Add a `Status: ... YYYY-MM-DD` line or move to closed:

- **D043** [section] — `mentioned_assets` multi-asset convergence
- **D055** [section] — Site repo node_modules disappeared
- **D075** [section] — Opportunity Scout Agent (business-idea discovery, separate from Agent A)
- **D059** [table] — SSG deploy throttle (pattern-copy of asbestos throttle at commit 3ed57dc)
- **D-SSG-01** [table] — 11 remaining asbestos path references in `run-batch.sh` (all in COMMENT lines)
- **D-SSG-02** [table] — Operator review of `SSG_TEMPLATES.md` Cosmetic Tolerance List + Verdict Rules
- **D-SSG-03** [table] — Confirm `relatedLinks` slug shape in SSG content
- **D-SSG-04** [table] — Reconcile AS1-AS5 anti-sameness checks between `SSG_TEMPLATES.md` and `audit_guide.py`
- **D-SSG-06** [table] — `AUDIT_SPEC.md` SPC-1..14 (~160 lines) still has UST/asbestos vocabulary
- **D-SSG-07** [table] — Stale "2,000-2,500 words" echo on `run-batch.sh` line ~357
- **D-SSG-08** [table] — `ctx.sh` has 22 asbestos references
- **D-SSG-09** [table] — `audit_guide.py` has 5 asbestos references in docstring/comments

