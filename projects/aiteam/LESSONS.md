# AITEAM Lessons Learned

Growing log. Each ctx save appends lessons under a date heading.

---

## 2026-05-15

- Verify wrapper model attribution BEFORE shipping cost-sensitive decisions. `ai-do.sh` silently used Opus (Claude Code default inheritance) and invalidated step 17 v1's "Sonnet wins" tier test. Pin `--model` explicitly in every wrapper.
- Bash heredoc `${5:-{}}` has brace-matching ambiguity — corrupted 16 audit_log payloads with a literal trailing `}`. Use `PAYLOAD="${5-}" ; [ -z "$PAYLOAD" ] && PAYLOAD="{}"`.
- `claude -p --output-format json` returns `total_cost_usd` and per-model `modelUsage` directly. No need to maintain a pricing map. Trust Claude Code's own numbers.
- Claude Code routes some work to Haiku even on Sonnet/Opus calls. Per-tier cost logging requires one row per model in `modelUsage`, not per call.
- Operator-suggested fixes deserve verification before literal implementation. The "export AGENT_ID env var" suggestion wouldn't have stuck — wrappers do `AGENT_ID="${2:-shell}"` which shadows env. Pass as `$2` instead.
- Schema mismatches in specs vs reality silently break SQL. The ctx-system spec used `timestamp`/`cost_usd`/`title`; real schema is `ts`/`estimated_cost_usd`/`description`. Schema-check before writing queries.
- Empty-data graceful handling is load-bearing for credibility. `/weekly_review` early-exits when <2 diary days. Better to honestly say "not enough data" than hallucinate.
- macOS bash is 3.2.57. No `${var,,}`, no assoc arrays, no `mapfile`/`readarray`. `${var/pattern/}` works. Process substitution works. `case` patterns work.
- Append-only audit logs are recoverable; mutable ones aren't. When a bug corrupts history, write a corrective row pointing back. Never UPDATE/DELETE. The corruption itself is a fact worth recording.
- Round-2+ discussion prompts MUST explicitly require engagement with a specific prior claim. Without this, multi-round devolves into parallel monologues — each agent restating its position.
- Document gaps only after verification. Initially documented `rubric.md` as not git-tracked; it actually was (commit `8ffd923`). Verify before claiming.
- Cost cap discipline: budget for retries. A client-side JSON parse error in step 18 burned a 3rd run on a 2-run cap. Save to file first, parse second.

### Session 2 — Curator + Briefer + /important (evening)

- all-MiniLM-L6-v2 sentence-transformers scores cluster low (0.3-0.6 range) for short-query-vs-long-anchor matches. Don't set a flat 0.50+ threshold without calibrating against real query/anchor pairs. Working values for curator: 0.35 slug→thesis matching, 0.15 group-mean placement. Top-1 was correct in 5/5 off-threshold cases — ranking discriminates well even when absolute scores look "weak."
- `close` is a reserved word in awk (builtin function). Using it as a `-v` variable name produces a misleading "syntax error" at the WRONG line. Cost the curator $0.28 on responses we couldn't parse. Use `startmark`/`endmark` or any non-builtin name.
- `paste -sd ", " -` on BSD/macOS treats the delimiter argument as a cycling list of single chars, not a multi-char separator. To join with ", " use `tr '\n' ',' | sed 's/,/, /g'`.
- Persist LLM responses to disk BEFORE parsing for any agent with non-trivial per-call cost. A parse-only bug becomes free to retry. Pattern: write `workspace/responses/<correlation_id>.md` first, parse second.
- `log_to_audit.sh` embeds the payload in a single-quoted SQL string with no escaping. Any `'` in the payload breaks the INSERT. Workaround: pipe payload through `sed "s/'/''/g"` before passing. Real fix: D044.
- Opus on `claude -p` does NOT internal-multi-turn like Sonnet at max-turns=30. Opus wrote 1.7K output tokens and stopped on the analyst prompt; Sonnet writes 12-13K. Result: Opus run cost LESS than Sonnet ($0.20 vs $0.26) on identical inputs. Don't assume "Opus is always more expensive" — benchmark on the actual prompt.
- Idempotent setup scripts must distinguish "seed" from "reset." `seed_taxonomy.sh` unconditionally overwrites; wiped Casper btc.md mid-checkpoint-4 (the curator had already accreted rows there). Fix at D042: write Casper outlook-history files only if missing or `row_count: 0`.
- Spec text can drift from spec body. The curator taxonomy spec said "24 topics" header / 27 in body / 28 after addition. When this happens, the body is authoritative.
- Model-override briefs (`_opus.md`, future `_haiku.md`) are operator spot-check artifacts, not pipeline inputs. Discovery globs across the pipeline must exclude model-suffix files explicitly. Tonight's filter: `grep -Ev '_(opus|haiku)\.md$'`.
- Telegram outbound via `parse_mode=HTML` is the lightest path for monospace grids: `<pre>...</pre>` for the grid, escape only `<>&` in regular text. Avoid MarkdownV2 — every `_*[]()~>#+-=|{}.!` would need escaping.
- Cron-spawned outbound messages need direct Bot API calls via curl. grammy's `ctx.reply` only works inside inbound handlers. Don't try to bridge through the long-polling bot process.
- `AGENT_MAX_TURNS_OVERRIDE` pattern (mirrors `AGENT_ID_OVERRIDE`): set inline before invocation, wrapper applies after `source .env`. Use for non-agentic text-in/text-out callers (curator topic updates, briefer compose) to pin max-turns=1 without globally editing .env.
- MEMORY-stored operator preferences need active recall. Operator's "put output files in ~/Downloads/" preference was relevant to D032's blind-comparison artifacts but I didn't apply until asked. For future similar phone-readable artifacts (D032-style spot-checks, comparison files, anything for grading), copy to `~/Downloads/` proactively.
- Seed-description embedding style coaching from operator: descriptive ("Cowen covers X using Y framework") embeds better than positional ("Cowen believes X means Y") for similarity-index purposes. Apply to future seed taxonomies.
- When claiming a D-number for a deferred item, check whether the operator may have intended that number elsewhere — especially when D-numbers haven't been ratified in HANDOFF/DEFERRED yet. I claimed "D043" for log_to_audit SQL-escape; operator had intended D043 for ETH/SOL convergence. Renumbered to D044.
- Bash `set -uo pipefail` (no `-e`) lets compound conditions like `[ ... ] && { ... ; exit; }` work without surprise exits, BUT a chained `&& [ ... ] && [ ... ]` evaluates left-to-right and can silently return non-zero from the last test. Use explicit `if` blocks for safety.
- macOS `sed -i` requires an argument: `sed -i '' -e '...' file`. The empty backup-suffix string is required; GNU sed pattern `sed -i -e '...'` fails on macOS. (Hit during curator's row_count frontmatter update.)
- jq's `--arg` escapes content correctly for JSON values but the resulting JSON, when embedded in single-quoted SQL by `log_to_audit.sh`, still breaks on literal `'`. The escape boundaries are: jq → safe JSON; sed `s/'/''/g` → SQL-safe-via-SQLite-double-single-quote convention.

### Brain vault git-init (later that day)

- Pre-flight checks catch blockers before they're expensive. Scoping `~/brain/` size, existing git state, existing SSH state, and crontab state up front flagged the missing `~/.ssh/` directory BEFORE we got to step 6 (push) where it would have stalled the chain.
- Verify operator-stated assumptions before relying on them. The build prompt said "the ust-contractors repo uses SSH on this machine, so the key should already be there." It wasn't. Don't pattern-match to "this should already work" — check.
- Pause points are leverage. Reporting the staged file list before the initial commit gave the operator a chance to opt `dashboard.log` and `inbox/` out of permanent history.
- `ssh -T git@github.com` exits 1 on success. GitHub doesn't provide shell access, so SSH disconnects with exit 1 after authenticating. Look for `Hi <user>! You've successfully authenticated` in stdout, not exit code 0.
- `ssh-keyscan -t <key-type> <host> >> ~/.ssh/known_hosts` is the right way to pre-populate host fingerprints for non-interactive SSH. Avoids the interactive prompt without disabling host verification.
- Specific token-pattern regex beats loose secret-keyword grep for pre-commit scans. `(sk-[a-zA-Z0-9]{20,}|ghp_[a-zA-Z0-9]{20,}|xoxb-[a-zA-Z0-9-]{30,}|AKIA[A-Z0-9]{16})` catches actual credentials; `secret|api_key|password` returns false positives from any doc mentioning secrets management.
- Per-repo `git config user.name` works without `--global` — useful for machines hosting repos under multiple identities. The `--local` flag is implicit when run inside a repo. Don't pollute global identity for one project.
- `git rm --cached <file>` (with `-r` for dirs) removes from index without touching the working tree. Correct unstage pattern when adjusting gitignore mid-build.

### Phase 2 Telegram stack (later same day)

- grammy ≥1.21 moved file-download helpers to `@grammyjs/files`. `ctx.getFile()` in core returns a bare `File` object — no `.download()` method. Wire `bot.api.config.use(hydrateFiles(bot.token))` after bot construction. Without it, voice download dies with `file.download is not a function`.
- faster-whisper on Apple Silicon does NOT use Metal/Neural Engine. CTranslate2 is CPU-only on macOS, using int8 AVX/NEON kernels. Still ~1.8x realtime for medium model on M4 Pro (22s audio → 11.7s). True Metal acceleration is whisper.cpp or mlx-whisper, not faster-whisper. Don't trust prose claims about ANE/Metal — verify with a real timing test.
- macOS TCC denial on external volumes presents as EPERM, not EACCES, even when Unix perms are correct. Errno discriminates: EPERM = something above filesystem (TCC/sandbox/SIP/file flags), EACCES = Unix perms. On macOS 13+ "Removable Volumes" is a separate TCC category from "Full Disk Access," and the responsible process for a CC child is the `claude` CLI binary, not Terminal — granting FDA to Terminal doesn't transfer.
- Non-interactive `claude -p` sessions inherit default permission policy and deny most Reads on script files. Phase A "what's our SSD mount path?" dodged this via `Bash(df)`; Phase B voice queries like "what's our token spend today?" hit it immediately. The orchestrator surfaces denial in chat as "I need read permission for…" — that's an LLM honestly reporting being blocked, not a bot bug.
- iOS autocorrect capitalizes the first letter of a sentence. Telegram delivers `Confirm` when user types `confirm`. Keep safety-critical accept paths strict (autocorrect → cancel is safe-fail); relax stray-detection to case-insensitive on the lone word (false-positive cost is just suppressing a reply, not auto-routing a destructive command).
- Verify audit_log against the bar list before requesting more test inputs. Phase B sign-off: operator's 3 voice notes already hit every bar including the 10-min timeout. I asked for 3 more — wasted their time. Same rule applies to sign-off claims: query the data, don't trust the claim. Both directions.
- Telegram voice notes are `.oga` (Opus-in-Ogg), not `.ogg`. faster-whisper handles via ffmpeg internally, so the wrapper just needs to accept either extension. ffmpeg is a hard runtime dep for non-WAV input — `brew install ffmpeg` is part of the install recipe.
- Soft budget caps with cost-after audit beat hard pre-caps for LLM pipelines. The /ctx pipeline runs ai-do.sh, then queries `token_usage` by correlation_id post-call, audits `ctx_over_budget` if exceeded, but always completes. Pre-estimating token cost is unreliable; complete-and-flag is more useful than refuse-with-guess.
- `.bak` files written by tooling will get auto-committed to git if not in `.gitignore`. The /ctx skeleton-backup ended up in the brain repo. Add to gitignore proactively when any pipeline writes sibling `.bak` / `.tmp` files.
- Single-chokepoint security gates beat parallel implementations. Phase D's destructive scan lives in `routeAndLog()`, called from text and voice-confirmed paths both. One scan, one audit, no risk of a future path forgetting to gate.
- The "responsible process" model in macOS TCC generalizes beyond removable volumes — whenever an EPERM appears with valid Unix metadata, ask: which app is TCC tracking as responsible for this child process? It's often not the parent terminal.

### Market Scribe session 1 (later same day)

- YouTube channel handles drift. Benjamin Cowen's channel was `@intothecryptoverse` historically; now `@benjaminjcowen`. The freed handle was taken by squatter accounts (2-sub orphans). `forHandle=<old_handle>` returns HTTP 200 with empty `items[]` — not a 404. The resilient pattern: `search.list?q="<channel name>"&type=channel`, then verify by subscriber count and `customUrl`. Always capture both `id` AND current `customUrl` at watchlist creation time so future audits can spot drift.
- macOS bash 3.2 bug — unquoted heredoc + variable expansion + apostrophe in body produces cascading phantom quote-matching errors. `PROMPT=$(cat <<EOF ...${VAR}... what's ... EOF)` triggers `unexpected EOF while looking for matching` at lines far from the actual offense. Fix: pull static text into a separate file and substitute via `${VAR//pattern/replacement}`. Quoted heredoc (`<<'EOF'`) also works but disables variable expansion.
- YouTube auto-VTT rolling captions need real overlap dedup, not exact-match. Each phrase appears across 2-3 cues with sliding boundaries. Algorithm: for each new cue, find the longest tail of the prior cue that matches the head of the current cue, emit only the suffix beyond that overlap. Threshold of 5 chars avoids false-positive matches on trailing punctuation. 3x size reduction on real video transcripts.
- YouTube API descriptions contain raw control characters in U+0000-U+001F that break jq strict mode. Pre-clean with `tr -d '\000-\037'` before piping to jq. HTTP 200 returns with malformed JSON; jq errors, Python's `json` is more lenient.
- Sonnet via `claude -p` with default `--max-turns 30` does internal multi-turn reasoning even for pure text-in/text-out tasks. A 1.5K-token visible brief can ring up 12K+ output tokens internally. For non-agentic tasks, a `--max-turns 1` variant (e.g. an `ai-do-single.sh` sibling) would cut cost ~70%.
- bash `${VAR//pattern/replacement}` handles multi-line $VALUE content cleanly — better than sed for template substitution when the replacement is a markdown transcript or filled template.
- Per-pipeline cost attribution generalises: `<pipeline>_<purpose>` agent_id naming (`telegram_bot_orchestrator`, `analyst_cowen`, `analyst_casper`) makes the dashboard's per-pipeline rollups trivial. The Phase 2 AGENT_ID_OVERRIDE env-var pattern is the right hook.
- `yt-dlp` filenames are templated as `<base>.<lang>.vtt`, not fixed — use a glob (`ls "$base".*.vtt | head -1`) to find the produced file, since the language code can vary.
- Telegram doesn't render markdown tables. The on-disk format and the Telegram format need different renderers for the same data. On-disk markdown (Obsidian rendering) stays as tables; Telegram presentation is monospace code-block grid with em-dashes for blanks. The presentation layer is downstream from the storage layer — keep them decoupled.
- The analyst's start-of-spoken-thought timestamp attribution for quotes (concatenating VTT cues to sentence boundaries) is *desired behavior*, operator-confirmed. Strict line-by-line verbatim produces fragments; sentence-level produces useful quotes. Future iterations of any transcript-to-quote pipeline should preserve this.

---

## 2026-05-16

### Brain audit → diary cron fix → tg-monitor full build (late 2026-05-15 → early 2026-05-16)

- Schema-mismatch lessons from prior sessions re-validated in real time. Same session: hit `no such column: payload` (real column is `payload_json`) AND silent-match-everything on `ts >= '2026-05-15'` (ts is INTEGER epoch; SQLite type-coerces `'2026'` to 2026, so the predicate matches all rows). Both were warnings in LESSONS from yesterday. Lesson holds: when reaching for any `aiteam.db` table, run `.schema <table>` first or refer to the column list — don't recall from memory.
- Self-documenting code can lie. `write_diary.sh` line 4 said `cron: 55 23 * * *` but the line was never in any actual crontab. Pattern: any time a script claims to be scheduled, check `crontab -l` against the claim. Header comments document intent, not reality.
- `git add <file>` silently bundles ALL pre-existing uncommitted changes to that file, not just the changes you just made. Caught only because `git commit` reported "20 insertions, 7 deletions" for what should have been a 1/1 diff. Defensive pattern: `git diff <file>` before `git add <file>` when the file is touched by tooling you don't control end-to-end. Recovery: `git reset --soft HEAD~1` is non-destructive — re-commit with an accurate message; no `--amend` needed (and `--amend` is off-limits without explicit operator request anyway).
- Interactive scripts require operator hand-off via `!` prefix. The Bash tool runs commands non-interactively — `nano`, Telethon SMS-code prompt, anything with a TTY-required input loop will block forever waiting for keystrokes that never come. Detect early ("this script needs input typed on a real terminal"), generate the script, hand off with explicit `! cd ... && ...` instructions.
- Cognitive blind spot when re-typing similar-looking content. Dropped the `cd /Users/mmm2/agents/tg-monitor && ` prefix from the reader cron line three times in a row, even with the spec visible — including once on a deliberate "rewrite from scratch" attempt. Pattern: when content has repeating long similar lines, do not re-type them. Use `Write` to control bytes exactly, or `Edit` to make a single surgical change.
- Telethon `iter_messages(chat_id, min_id=high, limit=N)` is the correct forward-only paginator across cron runs. Returns up to N messages with id > high, newest-first. Store `MAX(tg_message_id)` per chat_id, pass as `min_id` next run. Bootstrap with `min_id=0` returns the most-recent N from history — a snapshot, not a backfill.
- 200-message bootstrap covers wildly different time windows depending on group velocity. 5 groups × 200 = 1,000 messages, but only 47 fell inside the 24h analyzer window — the other 953 stayed `analyzed_at IS NULL` forever. Intentional per the spec's "forward only" stance, but worth documenting: bootstrap is a snapshot, NOT a historical backfill.
- Two SQLite files in the same process need separate connections and separate commits. The tg-monitor reader holds `conn` (messages.db) + `archive_conn` (archive.db) simultaneously, never shares a transaction. Each `commit()` is targeted to its own DB. Critical: archive failure must NOT roll back messages.db. Wrap archive logic in `try/except`; messages.db path is load-bearing, archive is bonus.
- `sqlite3.Cursor.rowcount` after `INSERT OR IGNORE` is 0 if the row collided, 1 if it inserted. Use this to count fresh inserts. Different from `total_changes` on the connection, which is cumulative across the lifetime — easy to misuse if you assume "changes since last operation."
- The Phase 2 bot outbound (`notify.sh`, shipped earlier same day) is solid — the tg-monitor analyzer's first live digest delivered first-try with no `outbox_fallback`. Validates the "reuse existing infra, don't build a parallel sender" decision for any future agent that wants to push to operator's phone.
- When the operator says "MAX 80 words total" in a prompt spec for a per-group summary, treat the cap as per-group, not for the entire digest (which would be ridiculous for 5 groups). The Sonnet prompt is invoked once per group; the cap applies to that one invocation's output.
- Cap semantics matter and should be operator-explicit: "captured today" (count against today regardless of original send time) vs "sent today" (count against day-of-send). For tg-monitor archive, chose captured-today because `local_date` is computed once at reader-run start. Simpler, deterministic, resets at local midnight. Worth surfacing in any README so the operator isn't surprised by a backlog burning today's cap.
- Cron heredoc bytes are fragile under cognitive blind spots. After 3 failed attempts via heredoc, switched to `Write` (controlled bytes) + `Edit` (single surgical line change) and the cron landed correct on the next try. Lesson: when a heredoc keeps producing the same wrong output, the tool isn't the bug — switch tools.
- `awk -F= '{print $1, "=", (length($2) ? "SET" : "EMPTY")}'` is the right "verify .env structure without leaking values" pattern. Comment lines and blank lines fall out as `# ... = EMPTY` and ` = EMPTY` respectively (they have no `=`), so the operator can mentally filter them out. Real keys with values show as `KEY = SET`.

### Verdict persistence + deploy throttle (late 2026-05-16)

- Build specs sometimes describe systems that don't exist yet. The Escalation Triage spec assumed an `audit_log.tier='R3'` schema and a drafter→auditor R-tier pipeline; none of that existed in the codebase. Pattern: when a spec names specific table columns, agent directories, or audit_log actions, `grep` for them BEFORE accepting the premise. A 10-minute recon prevents hours of building against a phantom upstream.
- When the upstream provides a `--dry-run` mode, use it as the queue source for any wrapper script. `ship.sh --dry-run` emits `[would-ship] <site>: <slug>` for the eligible set via the same `process_site()` path the live tick uses. Calling it from both `preview_ping.sh` and `deploy_batch.sh` means they cannot drift — same code path generates both lists. Rejected re-implementing the filter inline (would have created a second source of truth that future-me would forget to keep in sync).
- `ship.sh` always exits 0 even when per-slug deploys fail (`process_site` swallows `ship_one_slug` failures with `|| true`). Wrappers that loop over slugs and need per-slug success cannot rely on the exit code — must parse stdout for `[ship] <slug>: shipped` vs `[ship] <slug>: stage failed`. Documented inside `deploy_batch.sh`'s header so the next reader doesn't trip on this.
- `log_to_audit.sh` takes POSITIONAL args, not `--flag` style. Signature is `<actor_type> <actor_id> <action> <target> <payload_json> [correlation_id]`. Build specs that show `--actor-type X --actor-id Y …` are spec drift, not API. Convention is positional across the codebase (`ship.sh`, `run_tier_test.sh`, etc.).
- `audit_guide.py` writes `composite_score=NULL` because mechanical checks don't produce a composite — only the editor's 5-axis rubric does. Any UI/message renderer must handle the NULL case explicitly (display em-dash, not "0"; rendering 0 would be misleading). `preview_ping.sh` renders `—`.
- DEFERRED.md D-numbers collide silently. Build prompts that ask for "add D053 …" without checking the file are common, and `grep -nE "^### D[0-9]+" DEFERRED.md | tail` should be a prerequisite for adding any new entry. Follow the file's own "pick the next free D-number" rule, and flag the rename in the build report so the operator knows.
- Approved-guide JSONs store body content in `.paragraphs[]` (array of strings), NOT `.allText`. `.allText` exists in the schema but is empty string. Word count via `jq -r '.paragraphs | join(" ")' "$file" | wc -w` works; `jq -r '.allText'` returns 0 words for every file. Always confirm structure with `jq 'keys'` + per-field type check (`jq '{f1_type:(.f1|type), f1_len:(.f1|length)}'`) before assuming an accessor.
- Vercel ≠ Cloudflare. The asbestos pipeline deploys via `git push` to `main` → Vercel auto-deploy. No `wrangler.toml`, no `.cloudflare/`, no `vercel.json`, no `.vercel/` dir. When a build spec names a deploy provider, verify with `grep -r '<provider>' ~/projects/<site>/` before building around its assumed primitives (preview URLs, deploy commands, etc.).
- Sub-shell variable scoping bites in bash 3.2 pipelines: `cmd | while read line; do count=$((count+1)); done` runs the loop in a sub-shell, and `count` outside the loop is unchanged. Fix: write to a temp file inside the pipe, count with `wc -l < file` afterward, then iterate the file with `while read … done < file` (NOT a pipe — this form runs in the parent shell). Pattern used in `deploy_batch.sh`.
- Pre-flight recon should produce a concrete artifact the operator can audit before any build proceeds. For the verdict-persistence build, the recon report listed 5 candidate locations for R3 verdicts and confirmed all 5 were empty — this gave the operator everything they needed to pivot the scope in one round-trip instead of multiple "did you also check…?" exchanges.


### Pipeline-wrap session: F2 + F4 fixes + ship-to-site PATH repair (late 2026-05-16)

- Bash 3.2 doesn't expand `\'` inside double-quoted strings. Build specs that show `${VAR//\'/\'\'}` for single-quote escaping silently produce literal `\'\'`. Fix: hold the quote in a variable — `APOS="'"; "${VAR//$APOS/$APOS$APOS}"`. Always test the specific bash version (3.2.57 on Apple Silicon Mac) before trusting a spec's literal escape syntax.
- Cron's PATH excludes `/opt/homebrew/bin` on Apple Silicon Macs. Any cron-invoked script calling Homebrew binaries (npm, node, python3, jq, sqlite3, gh, etc.) needs a PATH prelude or breaks silently. Idempotent prelude: `case ":${PATH}:" in *":/opt/homebrew/bin:"*) ;; *) export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:${PATH}" ;; esac`. Tracked as D054 for a systemic audit.
- `npm run build` runs `next build` via PATH augmentation with `node_modules/.bin` — fine when node_modules exists, fails with `sh: next: command not found` when missing. The PATH prepend doesn't help because `next` isn't a Homebrew binary; it lives in the project's local node_modules. If a Next.js site repo's node_modules disappears (fresh clone, git clean, manual rm), every ship breaks until `npm install` runs. Tracked as D055.
- First-commit-of-a-living-agent is a recurring pattern. Both `ship-to-site/` and `assignment-drafter/` were running in production via cron without ever being in git. Easy to miss because the working tree is "complete" and the agent works — but a hard drive failure or `git clean -fdx` would erase the agent. Spec rule "agents land with their own commit at end of build" is the right invariant; defer-rule "commit when ready" leads here.
- The recursive-proof commit pattern works for self-healing infrastructure fixes. D044/F23 (log_to_audit SQL escape) was verified by logging the fix itself with an apostrophe-containing payload via the fixed script. Pre-fix, the row could not have been written. Audit row id 733 stands as the un-fakeable proof. Use this pattern when fixing observability infrastructure.
- `git add <path>/` stages everything under that path — when the path is an untracked directory, that's the entire directory. For the F2 commit this swept in `state/daily_pipeline_spend.txt` (a runtime tally), which got tracked. Always check what's actually in a directory before `git add`ing it wholesale.
- Operator-named D-numbers can collide with prior-session D-numbers. D045 collided between operator's "ai-do.sh rewire" (this session) and an older archived "Opus prompt-template" item from Cowen build. DEFERRED.md authoring should require `grep -nE "^### D[0-9]+" DEFERRED.md | tail` BEFORE adding any new entry. Flag any collision in the build report.
- The autonomous chain (drafter → run-batch.sh → ship-to-site) wasn't actually working until this session. Cron-driven shipping had been failing silently with `npm: command not found` since the agent landed; only the 31 manually-shipped guides ever made it live. Lesson: when a cron-driven pipeline claims "fully autonomous", verify by counting successful runs in audit_log, not by reading the build report.
- Verifying with byte-diff against a real artifact beats unit tests for substitution scripts. The provenance-strip verification (`diff actual_substituted real_shipped_wrapper`) caught a 2-line off-by error AND confirmed byte-identical output in one step. Always have a real-world artifact to diff against when validating codemods.
- `log_to_audit.sh` callers wrapping with `|| true` makes failures invisible. Every agent that wrote audit rows containing apostrophes was silently dropping them. The defensive scrub in config-synth's `handle_failure` worked around it; the lib-layer fix (D044/F23) is correct because it benefits every caller. Pattern: when callers reflexively swallow exit codes, the lib is shipping latent bugs.
- Spec line-counts that depend on a separately-authored doc are fragile. The `tail -n +23` in the page.tsx strip spec was off by 2 because my own extraction report count was off by 2; the user's prompt repeated my miscount. Lesson: when a number from a spec is questionable, count it again in the actual file before relying on it.
- 0-variance discoveries beat careful diffs. All 31 per-slug page.tsx wrappers are exactly 21 lines; byte differences are pure slug-length artifact. 3-way diff of diverse slug shapes reduces to 3 lines, all the slug substring. When a build expects per-page variance and finds none, that's often a strong positive signal about workflow discipline — not a bug.
- F2-style exit codes should be distinguishable: exit 2 for soft skip, exit 1 for hard error. Drafter currently treats any non-zero as a hard fire-failure, but the exit-2 hook means the next drafter.sh-touching session can route soft-skips into a separate bucket without breaking the existing failure path. Always leave room for downstream callers to distinguish.


### D044 caller-layer cleanup (2026-05-17 00:23 window)

- When a bug is fixed at the shared-lib layer, verify the fix is sufficient by stripping caller-side defensive duplicates ONE AT A TIME. Two layers look safer but mask which layer is actually working. If the caller-side scrub can be removed and the end-to-end test still passes, the lib is doing its job AND you've gained code clarity at zero risk. Used today on config-synthesizer's `tr "'" '_'` scrub after `log_to_audit.sh` got the proper SQL escape in 939361d.
- `audit_log` payloads make a free end-to-end test for SQL-escape work. Write an apostrophe-bearing reason string through `log_to_audit.sh`, query the row back via `sqlite3`, eyeball the preserved verbatim string. The live infrastructure IS the test — no mocks, no fixtures, no harness. Especially valuable because the original D044 bug was silent (the INSERT just dropped), so this round-trip test is how you'd know the lib fix worked.
- A `deferred_closed` audit event should record WHICH path was taken in its payload — e.g. `path: B`, `approach: crontab-PATH-header`, `defaults_changed: [WRITER_MODEL, AUDITOR_MODEL]` — and what's still belt-and-suspenders (e.g. `preserved: build_verify.sh per-script guard`). When a regression surfaces three months later, the question "did we keep BOTH defenses?" needs an answer in audit_log, not a memory test. Pattern used to close D045, D-SSG-05, D054 today.


### SSG pipeline discovery + scaffolding (2026-05-17 00:25 window)

- macOS Desktop is TCC-blocked for terminal apps by default. `~/Desktop/AITEAM FILES/`, `~/Desktop/CC UST FILES/`, `~/Desktop/CC ASBESTOS FILES/`, `~/Desktop/payroll-detective/` all return `Operation not permitted` to `ls`/`find`/`mdfind` from the shell. Multiple CLAUDE.md files document paths under `~/Desktop/<project>/` as authoritative — those paths are inaccessible to CC by default. Fixes: grant Full Disk Access in System Settings → Privacy & Security, or move folders out of Desktop via Finder (Finder isn't subject to the same TCC restriction). If a CLAUDE.md references a Desktop path the shell can't read, that's TCC, not a missing file.
- `mdfind` (Spotlight) bypasses TCC for read-only existence checks. Spotlight indexes Desktop too, so `mdfind -name "<filename>"` returning empty means the file truly doesn't exist anywhere indexable. Useful for confirming nonexistence system-wide before cloning from GitHub. Caveat: a Spotlight hit confirms existence in the index, not shell-readability — TCC-blocked content still indexes.
- The hyphenated `payroll-detective` content repo is LOCAL-ONLY. `git@github.com:mdp280028-ui/payroll-detective.git` returns `Repository not found` despite being referenced as "Source of truth" in `payrolldetective/CLAUDE.md`. It exists only at `~/Desktop/payroll-detective/` (TCC-blocked). Any future plan to consume Mandy's pipeline as a comparison template needs the operator to either grant FDA, push it to GitHub, or move it out of Desktop via Finder first.
- UST and asbestos pipelines have **forked**, not parent-child. UST is ahead on AUDIT_SPEC (v1.6.5 with SPC-16 UMBRELLA-KEYWORD-GUARD) + service spec (v1.7) + has `spc-audit.py`. Asbestos is ahead on run-batch (v10.13) + audit_guide (v3.4 with G1 SEMANTIC-VARIETY) + guide-spec (v2.7). Neither is downstream of the other. A "best-of-both merge" would need cherry-picks per file, not a rebase. For the SSG scaffold this session, asbestos was chosen as the base because guide production is the SSG starting mode.
- `cp -R` + `rm -rf .git` + `git init` + initial commit is a clean repo-fork pattern. Used for asbestos-contractors → ssg-content. Captures the entire baseline as one commit with no upstream-history pollution. Pairs well with moving content artifacts to a sibling `_reference/` folder so the new repo stays small and the originals remain queryable.
- When porting a content pipeline, distinguish references-in-pipeline-files from references-in-content-artifacts. SSG-content had 2,530 occurrences of "asbestos" but only ~80 were in pipeline files (run-batch, audit_guide, AUDIT_SPEC, CONTENT_SPEC_GUIDE, ctx.sh, auto-harvest-to-library); the other 2,400+ were inside approved guide JSONs, keyword research, assignment batches, per-slug configs. Content artifacts are delete-not-rename; pipeline files need targeted edits. Always count occurrences-by-file before deciding the edit strategy.
- `smartsourceguide` (the bare site repo) ships with zero docs/specs/keyword research, in contrast to asbestos which inherited a 374-mention keyword research report and 27 reference articles when forked from UST. Content production for the SSG track starts from genuine greenfield — no domain vocabulary, no authority-link inventory, no scope doc. This is a higher bar than the asbestos handoff.
- `gh` (GitHub CLI) is not installed on this Mac. `gh repo list mdp280028-ui` returns command-not-found. For session work that needs to enumerate or clone repos, either install via `brew install gh && gh auth login`, or use `git ls-remote git@github.com:owner/<name>.git` per-candidate (exit code 0 = exists, 128 = doesn't). Operator-provided exact repo names is the fastest path until `gh` is available.
- An empty `_template/` is not a template. `~/agents/_template/` contains only `.gitkeep` — zero scaffold files. The "scaffold for new agents" is conceptual, not on disk. New agents currently get hand-cloned from `editor/` or `librarian/`. If future work needs the scaffold to actually exist, it has to be authored — the directory is misleading as-is.

### POLICY.md lock + brain bookkeeping (2026-05-17 00:30 window)

- **Stale-deferred-item premise check belongs in pre-flight, not in execution.** D045 was scoped as "rewire writer/auditor from Opus → Sonnet, ~75% cost reduction". Two minutes of `grep "claude -p" run-batch.sh` + reading lines 107-108 (`WRITER_MODEL="--model sonnet"` is the default) showed the swap had already happened. Without pre-flight, would have shipped a no-op edit and falsely reported "~75% cost reduction landed". Rule: for any deferred item older than ~2 weeks, grep current state to confirm the stated premise before executing.
- **`ai-do.sh` is NOT a drop-in `claude -p` replacement for non-interactive callers.** Three blocker differences vs `claude -p`: (1) no `--dangerously-skip-permissions` pass-through — cron-fired pipelines would hang on first tool-use permission prompt; (2) hardcoded `--max-turns ${AGENT_MAX_TURNS}` (currently 30; was unlimited at callsite) — introduces a new failure mode for tool-heavy turns; (3) forced `--output-format json` + Python extraction of `.result` — fine for callers that ignore stdout (e.g. `run-batch.sh`), breaks callers that consume `claude -p`'s text stream. Any "swap `claude -p` for `ai-do.sh`" task needs a per-callsite flag audit. Carried into D061 build notes.
- **DEFERRED.md D-number collisions are silent.** Operator requested D060 + D061; D060 was already taken. Pre-grep rule: `grep -nE "^### D[0-9]+|^\| \*\*D[0-9]+" DEFERRED.md` (covers detail sections AND Priority view table) before assigning any number. File already has duplicate D045 (operator's + Cowen archived), duplicate D051, duplicate D027 — past silent collisions left in place.
- **Doc-vs-reality audits often find the gap is already mitigated.** 2 of 3 gaps in this session's audit were non-issues. "Cloudflare Pages" hits in `LESSONS.md`/`HANDOFF.md`/`docs/deploy_throttle_build_2026-05-17.md` were all *corrections* ("Vercel, not Cloudflare"), not stale assertions. "production judge" hits were all in historical context-saves. Reflexively editing would have *un*corrected already-correct docs or churned historical record. Rule: read surrounding context of every grep hit before editing — "Cloudflare" next to "Vercel, not Cloudflare" is mitigation, not bug.
- **Untracked files in `~/brain` trap commit messages.** `cross_agent_failure_modes_2026-05-16.md` was untracked when I edited it; committing the 7-line correction under "doc-vs-reality correction" would have actually committed 872 lines (whole file + correction) — commit message would lie about diff size. Pre-commit check: `git ls-files --error-unmatch <path>`. If untracked, either split into add-file + apply-fix commits, or update commit message to honestly describe the addition.
- **POLICY.md pattern: ≥3 cross-cutting operator decisions in one session → durable file, not context-save.** Context-saves are time-sliced narrative that decays as decisions get buried under newer sessions. POLICY.md is a single grep target (`grep "## Q2" POLICY.md` instantly answers "what's the daily cost cap?"). Future-CC treats POLICY.md as authoritative — do not re-derive Q1–Q7 in conversation. Reopening any of them requires explicit operator decision noted in DEFERRED.md.
- **`ctx.sh` mechanical record only covers `~/agents/`, not `~/brain/`.** Brain-only sessions produce blank mechanical records ("no commits since last save"). For brain-only sessions, the narrative section is the entire signal — empty mechanical block is not "nothing happened", it's "wrong repo for the question".


---

## 2026-05-23 → 2026-05-24

### Idea-agent + /model picker + notes-agent (text + image + forwards)

- **D-number pre-flight is racy across concurrent chats.** Sister-chat work added D084-D086 between my pre-flight check (start of session) and my commit time (hours later). My reserved D084 was taken. Pattern: pre-flight reports the proposed D-number, then RE-CHECK `grep -oE '^### D[0-9]+' DEFERRED.md | sort -uV | tail -1` immediately before writing the new section. Don't trust pre-flight numbers across long sessions.
- **`dotenv` reads `.env` from `process.cwd()`, not the script's directory.** `bot-launch.sh` exec'd node with an absolute path but launched from a non-telegram CWD; dotenv looked for `.env` in the wrong place, bot crashed with `FATAL: BOT_TOKEN missing in .env`. Pattern: any script that exec's a dotenv-using process must `cd "$(dirname "$0")"` near the top. Worth a fleet-wide audit.
- **Heredoc `python3 - INPUT OUTPUT <<PYEOF` helpers must explicitly write to `sys.argv[2]`.** First idea-agent synth smoke wasted ~$0.40 because the parser `print()`-ed to stdout instead of writing to the output file path. JSON-correct Sonnet output, but downstream `cat $OUTPUT` returned empty → `IDEAS_COUNT=0` → no inserts. Pattern: always `with open(sys.argv[2], "w") as f: ...` — never rely on shell-captured stdout from a heredoc helper that's also supposed to produce a file.
- **jq `\"\"` inside bash double-quoted interpolation is a parse hazard.** `jq -r '... \(.value.snippet // \"\")...'` was rejected with INVALID_CHARACTER. Use `+` concatenation OUTSIDE the interpolation instead: `... + (.value.snippet // "")`.
- **grammy `bot.on(filter, ...)` is TERMINAL by default.** Once `bot.on("message:text", ...)` matches, `bot.on("message", ...)` does NOT fire. Forward-detection can live INSIDE the message:text/photo handlers via `if (isForward(...)) return handleForward(ctx)` — no separate `bot.on(":forward_origin", ...)` needed, no double-firing risk.
- **grammy `bot.command()` accepts strings or string arrays but NOT regex.** For dynamic patterns like `/yes_<category>`, intercept in `bot.on("message:text")` BEFORE the unknown-command reject. Regex extract → dispatch.
- **`AI_DO_MODEL_OVERRIDE` env hook in `ai-do.sh` for per-agent overrides.** Matches existing `AI_DO_SKIP_PERMISSIONS` / `AGENT_ID_OVERRIDE` / `AGENT_MAX_TURNS_OVERRIDE` patterns. Backward-compatible (sonnet default), opt-in per call, invalid values fall back silently. Fleet convention: opt-in env hooks, not config files.
- **First-run cap for synthesis agents.** When prior-output history is empty, cap output at N (e.g. 5) regardless of pass count. First run is calibration, not delivery. Move overflow to a "rejected with `failed_test: first_run_cap`" bucket so the synthesis quality is still visible.
- **`claude -p @<image-path>` for vision — start with `max-turns=2`, optimize to 1 after smoke confirms.** Whether `@path` inlines as native multimodal (no Read tool turn) or requires one is not obvious from `claude --help`. Safe ceiling first, drop later.
- **bot.js → sqlite3 via `runShell` — JS-side APOS pattern: `s.replace(/'/g, "''")`.** Mirrors the bash APOS helper. Sufficient for code-controlled values; for user-controlled text, use `--separator '|'` + structured parse.
- **Forward metadata: handle Bot API 7+ `forward_origin` AND legacy `forward_from`/`forward_from_chat`.** Grammy 1.30 forwards both. `forward_origin.type` covers `user` / `chat` / `channel` / `hidden_user`. Default fallback to "unknown" — never crash on unfamiliar forward shapes.
- **Smoke writes that touch operator-visible storage need cleanup.** `capture.sh forward` smoke left synthetic test entries ("alice", "@founder_user") in operator's real inbox. Pattern: use `_test_` prefixed paths during smoke, or clean up explicitly. Or smoke into a separate inbox file.
- **When operator says "just build" after sign-off, drop the cross-reference reports.** Terse status updates only between explicit STOP gates. Save the structured reports for natural STOP points (pre-flight, smoke, build-complete).
- **Don't pre-register every dynamic command variant.** `/yes_<category>` could be 7 known + N improvised. Pre-register the 2 canonical (`/yes`, `/no`), catch the dynamic pattern in `bot.on("message:text")` via regex. Avoids the brittleness of a closed list.
- **Telegram forward of a forward (nested) — use outermost `forward_origin` only.** Don't try to unpack chains; Telegram doesn't preserve depth reliably. Outermost source is the operator-visible attribution.
- **Two end-to-end smoke runs is the right budget for synth-heavy agents.** First catches parser/wiring bugs; second confirms the fix. ~$1.10 total for idea-agent. Pattern: persist raw responses BEFORE parsing so re-parses don't burn new calls (already a fleet lesson — reinforces).

## 2026-05-17

### Watchdog hardening + tuning (2026-05-17 11:00-13:15 window)

- **Audit_log column name is `payload_json`, not `payload`.** Second occurrence of the same lesson — see line 87 entry above. Watchdog tuning recon 2026-05-17 hit the mismatch when the operator-provided spec query said `substr(payload, 1, 120) AS payload_preview`; CC silently substituted `payload_json` and noted it in the report. The "schema-check before queries" rule keeps being re-validated because spec authors recall the field as `payload`. Treat this as the canonical lookup: queries against `audit_log` use `payload_json`. Spec authors writing watchdog/audit queries should use `payload_json` from the start so CC doesn't have to silently correct.
- **Drafter heartbeat fires BEFORE kill switches consult flags — observed in the wild.** At 2026-05-17 13:02:02 a manual off-cycle drafter run wrote `drafter_tick_started` (audit id 1149), then `skipped_pause_flag` (id 1150) because `DRAFTER_PAUSED` was set in `~/store/flags/`. Watchdog correctly saw drafter as alive throughout. Validates the heartbeat-before-killswitch design landed in commit fb53d04 (`drafter: add per-tick heartbeat row for watchdog (D-WATCHDOG-PREP)`): the heartbeat signals "cron fired," not "work happened," and that's the right semantics for watchdog visibility. Operator pausing the work via flag does not look like a misfire to watchdog. Apply the same pattern to any future agent that needs both a kill switch and watchdog coverage — heartbeat first, kill switch second.

### Multi-cluster execution session (2026-05-17 11:00–13:55, ~9 work blocks)

- **Audit row anatomy: 5-arg form is non-negotiable.** `log_to_audit.sh actor_type actor_id action target payload [correlation_id]`. Passing 4 args with a JSON blob as the 4th lands the JSON in TARGET, not payload_json. D064 validation (commit `ebb0507`) now rejects `$# < 4`, ACTOR_TYPE containing `=`, and TARGET starting with `{` — exits 2 with specific stderr. The verify column is `payload_json` (not `payload`). Verified by 4 distinct test invocations; first malformed row caught + corrected this session was D066's row 1142.
- **Exit codes through pipelines need `set -o pipefail` or no pipe.** `cmd | head -5; echo $?` captures head's exit, not cmd's. D061 smoke test initially showed `exit=0` when ai-do.sh was actually exiting 1 — the pipe to head masked it. Future exit-code smoke tests: capture directly. `cmd > /tmp/out 2>&1; rc=$?; cat /tmp/out`.
- **`source .env` clobbers inline env overrides.** `COST_CAP_SOFT_USD=0.5 bash check_cost_caps.sh` had no effect because the script's `source ~/agents/config/.env` reset the var. Fix pattern: capture overrides BEFORE sourcing, prefer them after: `_OVERRIDE_SOFT="${COST_CAP_SOFT_USD:-}"; source .env; SOFT="${_OVERRIDE_SOFT:-${COST_CAP_SOFT_USD:-25}}"`. Same trap applies to any wrapper that sources .env.
- **`SYSTEM_PAUSED` is an env var, not a file flag.** Spec assumed `~/store/flags/SYSTEM_PAUSED` was honored by `check_kill_switches.sh` — it's not. Nothing in the codebase reads that path. The actual enforcement signal is `SYSTEM_PAUSED=true` in `~/agents/config/.env`, read by every consumer via `source .env`. `check_kill_switches.sh` is just an mtime-refreshed re-source helper. Hard-cap design: `sed -i ''` rewrites .env (the real signal), AND touches the flag file (idempotency marker only). Daily 00:00 cron flips both back. Drift detection on next cap-check.
- **Surgical-extraction pattern, repeatable.** When committing a feature in a file with unrelated uncommitted changes: `cp <file> /tmp/<file>-backup` → `git checkout HEAD -- <file>` → re-Edit feature hunks → commit → `cp /tmp/<file>-backup <file>` to restore unrelated work. Verify clean extraction: `git diff HEAD -- <file> | grep <feature-marker>` should be empty. Used 4 times this session (dashboard×3, drafter×1). Beats `git stash` for partial-hunk situations.
- **Surfaced-files-during-commit are expected when committing untracked directories.** After `git add directory/`, the next `git status --short` may show new untracked entries that were previously hidden under the parent `??`. D066 audit hit this 4 times. Build for it: after each directory-level commit, re-check status and decide if surfaced files are PRODUCTION, STATE, or SCRATCH before proceeding.
- **Pre-existing traps need adaptation, not replacement.** asbestos/ssg run-batch.sh had `trap 'stop_spinner' EXIT` set before stop_spinner is defined. Naively replacing with `trap 'rm -f $LOCKFILE' EXIT` clobbers it. Pattern: re-register combined traps after lockfile acquire (`trap 'rm -f $LOCKFILE; stop_spinner; ...' INT` etc.). On early-exit before stop_spinner is defined, `trap - EXIT INT` clears them to prevent `command not found` stderr noise.
- **ai-do.sh ≠ `claude -p` for cron callers.** Three differences a cron pipeline must account for: (a) `--dangerously-skip-permissions` not passed → cron hangs on tool-use prompts; (b) `--output-format json` forces JSON capture → stdout shape changes; (c) `--max-turns ${AGENT_MAX_TURNS}` imposes a ceiling. After D061 commit `5d68876`, `AI_DO_SKIP_PERMISSIONS=1` env hook solves (a). `AGENT_MAX_TURNS_OVERRIDE` env hook (pre-existing) handles (c) if real-world data hits the ceiling. Default 30 covers expected 10-20-turn writer/auditor calls.
- **Pre-flight discipline catches premise mismatches.** D052 spec asked about flipping `exit 1`→`exit 2` in drafter.sh top-level, but no `exit 1` calls exist there — the actual D-item body was about fire_pipeline.sh rc handling. F17/F18 hard-cap pause mechanism premise (touch a flag file) didn't match reality (nothing reads it). Both caught at pre-flight before any code was touched. The pattern: when a recon finds premise + reality mismatch, surface both interpretations to operator before coding.
- **Audit row malformed-row policy: insert sibling, never retroactively edit.** When a positional-arg fumble lands JSON in TARGET, the rule: insert a corrected sibling row with `supersedes:[bad_id]` in payload; cite canonical row in DEFERRED.md closure; leave bad row in place as honest history. Hit 3 times this session (D066 1142, D052 1151+1152). After D064 validation shipped, no new malformed rows landed.
- **Untracked-production-code is a recurring process gap.** 4th instance in one month (D066) — operator + sister-chat CC sessions write scripts directly into `~/agents/` without returning to `git add`, next session inherits the file as background state. D066 closed all in-scope files but the underlying pattern remains open; D066 itself stays as a meta-item with a triage-script in its body for the next hygiene-pass session.
- **`ctx.sh` mechanical record only covers `~/agents/`, not cross-repo work.** This session committed substantially to asbestos-contractors + ssg-content + brain — none of those show in the file-changed list of `~/agents/`. Cross-repo sessions need narrative sections to surface those commits explicitly. The mechanical block can be misleading when read alone for a cross-repo session.

### SSG recon + repo locator (2026-05-17 13:54-14:24 window)

- **`/mnt/project/` paths in specs are a Linux/cloud-sandbox tell.** macOS doesn't have `/mnt/`. When a spec references `/mnt/project/<filename>`, the doc was authored on Linux (likely a sister-CC chat running in a cloud sandbox) and isn't reachable from this machine. Translate to: search `~/Downloads`, `~/Documents`, the brain vault, or paste the content into the conversation. Don't waste a deep `find` against root.
- **Repo locator: missing keyword in repo name doesn't mean missing repo.** The SSG site repo is at `~/projects/smartsourceguide/` — but a recon assuming the repo would have "ssg" somewhere in its name greps to `ssg-content` only and concludes the site is missing. Defense: when looking for a domain's site repo, also grep by the product name, category names visible in known content (e.g. "fleet", "answering"), or inspect `git remote -v` of nearby repos to learn the org and infer sibling clones. Org `mdp280028-ui` was discoverable from `~/projects/asbestoshq-site` remote in 1 command.
- **State-file validity gating beats post-hoc detection.** Watchdog fix #5 (commit `1053913` this session, but earlier window) inserted a `jq -e 'type == "object"'` gate at tick start with flag-file dedup at `~/store/flags/WATCHDOG_STATE_CORRUPT`. Caught both malformed JSON and zero-byte cases; auto-clears on first clean parse. Pattern generalizes: any agent that depends on a parseable state file should validate-and-flag-or-bail at startup, not let downstream readers silently swallow parse errors (which becomes an alert-storm vector when the readers' empty returns drive state-transition logic).
- **Probe-vs-decision threshold duplication is a units-divergence trap.** Watchdog fix #3 (commit `38ed525`, grace_minutes wiring) had to land in FOUR places — 3 probe functions + main loop. A "minimal" fix patching only the main loop would have silently failed because each probe filters its own SQL/mtime threshold before signals reach the decision branch. When an agent computes the same threshold in multiple places (a DRY violation), changes to the formula must reach every site. If you can't refactor to a single helper this session, at least audit-grep every occurrence of the formula before claiming the fix works.
- **A/B dry-run with held-constant context is the cleanest "this code path is consulted" evidence.** For grace_minutes wiring, the literal spec test (`window=0 grace=1`, single run) could resolve either way without proving the field is read. Strengthened to two runs holding everything constant except grace_minutes (`window=0 grace=0 → missing`; `window=0 grace=35 → healthy`). A flip is direct evidence; a non-flip is a failed fix. Pattern: when adding a code path that consumes a previously-ignored input, prove it with A/B where only the input-under-test changes.
- **Spec premises drift fast; verify line-counts and "uncommitted" claims at pre-flight.** Earlier in this conversation a spec said `dashboard/server.js` had 384/12 uncommitted lines to commit. `git diff HEAD -- dashboard/server.js | wc -l` returned 0 — the work had already landed as `a7c2c71` during the same session via a sister-chat. The correct response was STOP-and-report, not commit-anyway. Any spec referencing specific line counts or "still uncommitted" state must be verified against current `git status` / `git log` before acting. Saves duplicate-commit / merge-conflict messes.

### D068 + D056 session lessons (2026-05-17 14:00-17:00 window)

- **D064 validator catches Claude-authored prompts, not just operator drift.** D068 session: wrote a 4-arg audit_log invocation with JSON in the TARGET slot; D064 validator (ebb0507) correctly rejected and forced a clean re-run. Canonical shape: `ACTOR_TYPE ACTOR_ID ACTION TARGET PAYLOAD_JSON` — 5 positional args. Watch for this pattern in CC prompts that include audit logging. Claude-authored prompts have the same arg-shape failure mode as operator commands.
- **Cross-repo SHA references need repo qualification.** D068 pre-flight expected `1e90165` (LESSONS.md doc commit) in `~/agents/watchdog/` git log. Real commit, real content, but lives in `~/brain` not `~/agents`. Reported as "phantom SHA," resolved at close-out. Going forward: when prompts reference SHAs across multi-repo sessions, name the repo explicitly — "1e90165 in ~/brain" not "1e90165."

### D056 + GEO Optimizer integration lessons (2026-05-17 16:00-17:35 window)

- **Whitelist coverage shields every approved slug from live gate testing.** asbestoshq-site whitelist (`src/components/GuideArticle.tsx`) contains all 32 approved JSONs — validate.sh short-circuits at `slug_already_shipped` BEFORE either editor (step 2.5) or GEO (step 2.4) gate runs. SQL-trace both branches becomes the only honest test path until a new pipeline slug arrives. When verifying a downstream gate, check the upstream gate's input space first — if it shields everything, plan SQL-trace + log-field assertions instead of live dry-run.
- **`score.sh || true` + re-query distinguishes hard error from fail.** ship.sh editor/GEO gate fires score.sh inline if no verdict exists, swallows nonzero exit with `|| true`, then re-queries auditor_verdicts. Hard error leaves no row → re-query empty → `*_no_verdict` skip; fail writes `passed=0` → re-query returns `0` → `*_failed` skip. Two distinct skip reasons from one fire-then-recheck flow. Works because score.sh persists the verdict BEFORE exit-code returns.
- **Burn-in distribution reveals corpus floors fast.** 4 editor scores across 4 format flavors (general / how-to / comparison / niche-product) immediately surfaced two ceilings (originality mean 2.75, hook mean 3.0 with zero variation) — composite range 3.20-3.80. No need for the 20-slug POLICY Q4 burn-in to see the structural pattern; 4 was sufficient to justify 3.8→3.6 tune. Don't over-collect data when 4-corner sampling already exposes the ceilings.
- **Stacked verdict gates can share computed locals.** When two gates (GEO at 2.4 + editor at 2.5) both need `src_json="${pipeline_repo}/$(dirname "${pipeline_glob}")/${slug}.json"`, declare `local src_json` once in the upstream gate; let the downstream gate reuse it. Otherwise the second `local` declaration silently resets the variable, then the second identical assignment re-computes the same value. Functional but redundant; cleaner code dedups. Flagged this as a spec deviation in commit `32b3ea3`.
- **score.sh owns its own fail routing.** When ship.sh gate hits a `passed=0` verdict, the slug is ALREADY in needs_review_queue (score.sh appended on its own fail path during scoring). The gate's job is just to log the skip + return — no double-routing. Important when reasoning about audit trails: the queue append correlates with the `editor_scored`/`geo_scored` audit row, not the `ship_to_site_skipped` row.
- **Inclusive threshold (`>=`) matters when scores land exactly at the bar.** white-asbestos-vs-blue scored editor composite exactly 3.80 — exactly equal to the original 3.8 threshold. `>=` made it PASS; `>` would have FAILed on a rounding edge. Document inclusivity explicitly in score.sh + commit messages so future re-tuning doesn't accidentally change semantics. Same logic applies to the new GEO threshold (3.0 default).
- **Predicted-floor rubric axes are corpus signals, not bugs.** GEO Optimizer Phase 1 pre-flight predicted tables + quotations would floor at 1 across the asbestos corpus (no structured presentations or named-expert quotes in the prose). Smoke test on white-vs-blue confirmed: TAB=1, QUO=1, other 6 axes 4-5. The rubric is correctly reporting genuinely-missing structure. Either operator wants to incentivize writer evolution (keep strict) or relax the axes; either is honest. Don't pre-emptively soften criteria to lift scores.

### Internal Link Agent v1 build (2026-05-17 15:00–17:36 window)

- **`ctx.sh` last-save window can be sub-minute.** If two ctx fires happen within 60s (this session hit 17:35 + 17:36), the second save's mechanical record is empty even though the prior save just rolled up the whole multi-hour session. Don't trample the earlier save's narrative slots — fill the later save's narrative + cross-reference the earlier save for mechanical record. Each chat keeps its own narrative file.
- **bash 3.2 is the Mac Mini default — no `${var@Q}`, no associative arrays.** Initial draft of `build_inventory.sh` used `${slug@Q}` quoting which would have silently dropped on apostrophes. Rewrote using env-var-pass to `python3` heredoc (matches existing `log_token_usage.sh` convention). All new agent scripts that need DB writes with user-controlled text should use the env-var → python3 pattern, not shell-quoted SQL.
- **Operator spec paths are aspirational; verify with `ls` BEFORE editing.** Build prompt referenced `~/agents/grammy-bot/bot.js` (doesn't exist) and `~/projects/asbestos-contractors/run-batch.sh` (actually at `content/run-batch.sh`). Always test path existence before opening Edit on it. The 2-second probe saves multi-minute debug.
- **Per-success hooks need a "kind" marker to distinguish first-deploy vs refresh.** `deploy_batch.sh` originally fired `gsc_submission_queue` INSERT per success — designed for first-time deploys only. When the internal-link refresh queue was wired in, the same success branch would have re-fired `propose_backlinks.sh` against an already-deployed slug, wasting ~$0.07 of Sonnet per re-ship. Solution: 3rd column in PAIRS_FILE: 'new' vs 'refresh'. Only 'new' triggers per-success side effects. Generalize: any per-success hook that's idempotent-only-for-new-content should gate on a kind marker.
- **`ship.sh` refuses if site_repo is dirty.** Any session that touches `~/projects/asbestoshq-site/` must commit before deploy_batch.sh can fire — even for a `--dry-run`. Order: commit-then-ship, not ship-then-commit. Caught when smoke-testing the deploy_batch.sh edit with stale GuideArticle.tsx changes still uncommitted.
- **`npm run build` + grep the prerendered HTML is the fastest UI-edit verification.** Next.js prerenders to `.next/server/app/<route>.html`. Greppable. Caught both type errors (compile-time) AND prop serialization (runtime render) for the GuideArticle.tsx suggestedLinks edit. ~30s end-to-end. Faster + more thorough than `tsc --noEmit` alone for any React component change.
- **Per-proposal Telegram approval intentionally diverges from ship-to-site's "auditor is the gate" rule.** Document the divergence in the new agent's CLAUDE.md with explicit rationale (stage-of-pipeline argument: backlink proposals are agent-authored edits to already-approved content, different gate). Future agents that add operator-approval-required steps should make the same divergence-or-not call explicitly, not by inertia from the build prompt.
- **Bot must be running for live `/command` tests.** Pattern: if `ps aux | grep node.*bot.js` returns empty, defer live handler tests to a future session where operator is set to start the bot. Don't autopilot a `node bot.js &` — operator may have it deliberately off (this session: bot had been off for 2 days). syntax-check + spawn-pattern-match against existing handlers is sufficient confidence to commit; full round-trip becomes a documented gap.
- **Smoke-test data can be real first data if the test method matched production semantics.** Step 10 smoke approved a real backlink proposal (chrysotile → white-asbestos-vs-blue-asbestos) via `apply_approval.sh`. The approval logic, JSON edit, queue append, audit-log row — all identical to what would happen if operator approved via Telegram. Operator chose to keep + commit it as the genuine first approval. Pattern: when build-time smoke produces semantically real output, ask operator before reverting — sometimes the cleanest thing is to keep it.

### Research/Opportunity Agent Session 1 build (2026-05-17 18:00-18:25 window)

- **bash 3.2 mis-parses apostrophes inside `$(cat <<EOF ... EOF)` heredocs even with unquoted `EOF`.** The outer `$()` parser scans for matching single quotes; a `don't` inside the heredoc body opens an apostrophe-quote that bash 3.2 then can't close, producing `unexpected EOF while looking for matching '`. Triage prompt cost 30+ seconds debugging before symptom-to-cause. Future heredoc-inside-`$()`: avoid contractions in the body, or assign with `printf` instead, or write the prompt to a file and read it back. Documented in `research-opportunity/CLAUDE.md` Hard Rules.
- **sqlite3 text columns can contain raw `\n`; bash `read -r ... <<<"$ROWS"` then splits at every newline mid-row.** Burned 22 wasted Haiku calls before noticing the symptom (Python `int(os.environ["ID_E"])` failing because ID was a body-fragment, not an integer). Fix: `REPLACE(REPLACE(col, CHAR(10), ' '), CHAR(13), ' ')` in every SELECT that feeds a bash shell-loop. Apply this pattern preemptively in any future bash-driven SQL extraction loop touching free-form text.
- **Sonnet sometimes returns JSON arrays even when prompted for a single object** — especially when the post contains multiple distinct pain points (1 Reddit post → 3 distinct pain questions). Single-object parsers must accept both `{...}` and `[...]` and pick the highest-relevance entry from the list. Also: Sonnet sometimes wraps in ` ```json ``` ` code fences despite "no code fence" instructions — always strip leading/trailing fence before json.loads.
- **PyYAML can't be installed system-wide on macOS Homebrew Python (PEP 668 externally-managed).** Per-agent venv is the precedent (tg-monitor pattern); spin one up with `python3 -m venv ./venv && ./venv/bin/pip install pyyaml`. Don't reach for `--break-system-packages`; the venv pattern is cheap and isolates the dep per-agent.
- **Smoke-state rollback after operator-impact tests is non-negotiable.** Approval handler smoke wrote a real line to `drafter_queue.txt` (operator-facing) and flipped two pain rows to queued/rejected. Snapshotted the queue file beforehand, restored after smoke, reset pain rows to 'unreviewed'. Operator's first real `/approve_pain` should land in clean state, not on top of smoke residue. Pattern: snapshot before, restore after, verify with `diff` — any test that touches operator-visible surfaces gets this treatment.
- **Pre-flight 2-pass recon (operator forks `STOP and report`, then deltas, then GO) prevents scope-drift mid-build.** Reddit-only recon → 9 forks surfaced; SE+YouTube delta recon → 3 more forks. Operator pinned all 12 before any code landed. Result: zero in-build operator interruptions, zero "wait, should this be X or Y" thinking time during code-writing. The discipline cost ~20 min of pre-flight that recovered ~60 min of mid-build churn.
- **Reddit public JSON works fine with a polite User-Agent at single-digit-req/min volume.** No PRAW, no app registration, no rate-limit hits across 4 test calls. UA form `aiteam-research-opportunity/0.1 (contact via repo issue)` is acceptable. The "Reddit blocks bots" lore applies at scrape-everything volume, not at "one multi-sub search per hour."

### Research/Opportunity Agent Session 2 build (2026-05-17 18:30-19:25 window)

- **`source ~/agents/config/.env` is a fleet-wide single-point-of-failure.** Session 2 hit a stray SHA on line 45 of `.env` (no var-name prefix) that bash tried to execute as a command when sourced. Every script that calls `source ~/agents/config/.env` was tripping, including `ai-do.sh`, breaking every Sonnet call. The symptom showed as "Sonnet call failed for id=X" — set -euo pipefail makes the source error look like the caller's fault. Defensive pattern: source .env into a subshell first, check $?, error explicitly if nonzero. Even better: add `validate_env.sh` to startup/cron-init paths that rejects any line not matching `^[A-Z_]+=.*$` / `^#` / blank.
- **Haiku CLI (`claude -p --model haiku`) has ~$0.025 fixed overhead per call** vs ~$0.001 raw-token cost. Session 2 spent $3.83 across ~149 triage rows = $0.026/row. The 30x gap is the agent-loop wrapper (system prompt, CLAUDE.md context, tools, agent-cycle overhead per invocation). For per-row triage at scale, batch multiple rows into one Haiku call OR drop the `claude` CLI agent wrapper and call the anthropic SDK directly. Don't budget Session N+1 triage cost using raw-token arithmetic alone — you'll be off by 30x.
- **Haiku-4.5 has a persistent topic-context-leakage bias for YouTube comments.** Strict CRITICAL RULE + worked examples + tighter scoring guide reduced but did NOT eliminate praise comments scoring 0.70-0.80 when the video happens to be asbestos-related. Appears to be a model-level preference Haiku-4.5 won't override via prompt-only fixes. Mitigation in v1: Sonnet extract is a reliable second gate (correctly downscores praise to 0.10 with reason "No homeowner pain question present — pure praise"). If false-positive volume becomes annoying in real digests, raise YouTube-specific threshold from 0.5 to 0.85 in config.yaml (~30% of YouTube comments cross 0.85; ~12% cross 0.7-0.84 mostly-praise band).
- **Stack Exchange unauth ignores `pagesize=25`, returns ~50 items anyway.** SE silently caps unauth pagesize at its default (30-50) regardless of the param. Worked in our favor here (more data, same quota cost). Don't rely on `pagesize` for predictable bandwidth control on SE unauth calls — register an app if you need finer control.
- **YouTube two-step quota dance is worth the 1-unit premium.** `search.list` (100 units, returns up to 25 video IDs) → `videos.list` batch (1 unit, returns viewCount for all 25) → filter views>10k → pin top 10. Total 101 units per monthly discovery. Skipping videos.list saves 1 unit but loses the viewCount-quality filter — too cheap to skip.
- **`UPDATE last_polled_ts=NOW` on `commentsDisabled` 403 is mandatory.** Otherwise the next hourly poll retries the dead video, burns 1 quota unit per tick, never resolves. For our pin list of 10 with 1 disabled video: saves 24 wasted units/day. Generalize: any external-API resource that can become permanently-unavailable needs a sticky-skip marker in local DB, not just transient retry logic.
- **Phase-N cron flip smoke-tests via proxy-fire with PATH-restricted env**, not by waiting for natural :00. `PATH=/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin <script>` captures the cron-environment risk in 30 seconds vs 40 min waiting for scheduler. Don't wait for natural cron unless you specifically want to test the scheduler itself (which we don't — cron is OS-tested).
- **Deterministic digest beat Sonnet-synthesized digest on reflection.** Pre-flight had Sonnet writing the digest text; in-build chose Python heredoc instead. Reasons: operator wants reliable phone formatting more than creativity; deterministic = $0 per digest; no risk of Sonnet wrapping in ` ```json ``` ` or refusing or hallucinating /approve_pain hints; trivially testable. Sonnet can re-enter for "What's interesting this week" editorial if ever wanted, but core digest staying template-driven is the right call. Generalize: digest/notification surfaces where stability+structure matter more than prose, prefer deterministic over LLM.

### Keyword Registry build (2026-05-17 19:30-22:00 window)

- **bash 3.2 mis-parses `case` inside `while` inside `$()` with quoted patterns.** Constructing `*"$VAR"*) ... ;;` produced `syntax error near unexpected token ';;'` despite well-formed syntax. The triple-nesting confuses bash 3.2's parser; the outermost `$()` scans for matching `)`, while-loop subshells add another layer, and case's `;;` terminator gets mis-attributed. Fix: escape to a Python heredoc one-liner for the match logic. General rule: when nesting `case`/`while`/`$()` with quoted patterns, jump to Python before fighting the parser. Cost ~3 min of debug + one re-edit.
- **A "pre-Sonnet shortcut" needs a pre-Sonnet keyword candidate.** Operator spec said "registry says keyword covered → mark our_coverage=full, skip Sonnet." But `proposed_kw` is what Sonnet GENERATES — it isn't available pre-call. Resolved by matching against the POST TITLE (the only pre-Sonnet signal). Title-only is conservative-by-design; YouTube comments (no title) always fall through to Sonnet. Documenting in case a future shortcut spec hits the same ambiguity: clarify *which field* gets looked up and *at which point* in the pipeline.
- **Markdown tables scale to 162 rows in Obsidian with no UX problems.** Pre-flight worried about row count. Reality: 176-line file, alphabetical sort, scans cleanly on phone. The cap is closer to 1000-2000 rows than 50. Plan accordingly when designing future brain-tracked agent outputs.
- **"Auto-maintained — do not edit manually" header is now standard for brain-tracked agent outputs.** Operator workflow includes casual Obsidian edits. Without an explicit "auto-maintained, your changes will be overwritten" notice at the top of the file, hand-edits will silently disappear on the next regen cron tick. Apply this pattern to: keyword_registry.md, pain_points_log.md, future digest archives, and any new agent that writes to ~/brain/projects/aiteam/.
- **Alias-collapse is correctness-critical even with only ~10 alias rules.** Without it, "chrysotile" (the term) and "white asbestos" (the alias) would show as two separate rows with ~120 + ~40 mentions respectively, hiding the fact that they refer to the same concept. With it, one canonical row shows the true site-wide coverage (160 mentions). Aliases earn their keep at very small N. Defer-aliases-to-v2 is the wrong call any time the corpus uses alternate terminology for the same concept.
- **Single helper script with multiple verbs (`shipped`/`refreshed`/`backlinked`/`lookup`) beats 4 separate scripts.** Bash callers don't need to know which file to invoke. Stable surface lets v2 swap full-regen-per-call for per-row UPDATE without touching any caller. Generalize: any agent exposing 3+ related operations should consider single-script + verb dispatch instead of multi-script.
- **Sister-chat git activity surfaces in `ctx.sh` mechanical record.** Today's save shows `b873996 feat(backlink-prospector)` from a sister chat that ran in parallel. Useful signal — let me know there's overlapping work to recon. Pattern: skim git activity in ctx record before writing narrative, flag sister-chat commits explicitly so future-self knows what NOT to attribute to current chat.

### Fleet health-check + OAuth-from-cron fix + market pipeline activation (2026-05-23 21:55 – 2026-05-24 09:30 window)

- **`claude -p` exits rc=1 from any non-interactive context that lacks OAuth credentials, and ai-cheap.sh/ai-do.sh's `set -euo pipefail` swallows it silently.** Symptom: `analyzer.log: ERROR haiku batch 1 failed rc=1 stderr=` — no JSON, no traceback, nothing to grep. Failure mode survived 7 days of broken 07:00 cron because the `delivery=telegram_bot` line in the digest still printed (classified=0 = no-op delivery). Fix: long-lived `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token`, sourced into the cron env via config/.env. Future-CC: any cron caller of claude -p needs this env var pre-set or it WILL silently fail.
- **`KEY=          # required` in .env files is bash-parsed as empty value (`KEY=""`), not as "TODO fill me in".** bash treats `#` as start-of-comment immediately after whitespace. When such an empty value is EXPORTED to a child process, claude exits rc=1 with "Not logged in" — strictly worse than the unset case (which exits rc=0 with the same JSON). Two export-leak paths to watch for in any fleet: (a) `set -a; source .env; set +a` blocks that auto-export every var sourced during the block (the `set +a` doesn't un-export, only stops further auto-export), (b) python-dotenv `load_dotenv()` writing to `os.environ` followed by `subprocess.run(env=os.environ.copy())`.
- **Cron-equivalent reproduction recipe: `env -i HOME="$HOME" PATH=/opt/homebrew/bin:/usr/bin:/bin bash -c '...' < /dev/null`.** The `< /dev/null` matters — without it, the test stdin inherits from the Bash tool's pipe which doesn't match real cron's stdin behavior. Tested empirically: `claude -p < /dev/null` returns "Not logged in" where the same call without redirect inherits enough to authenticate. Saved 20+ min of "why does it work for me but not in cron" debugging.
- **macOS cron does NOT catch up on missed firings when the machine wakes from sleep/reboot.** Every cron line that was scheduled to fire while the Mac was off is permanently skipped. Days-off counts of digests/diaries/auto-commits are gone forever, not deferred-and-replayed. Future-CC investigating "agent didn't fire": first check uptime, then check if downtime overlapped scheduled fire times.
- **macOS launchd `KeepAlive { SuccessfulExit: false }` means clean SIGTERM won't auto-restart.** Trade-off: operator can manually stop without launchd fighting them, but a clean SIGTERM from any cause (manual stop, OS sleep handling, `launchctl stop`) leaves the service down until explicit restart. For bots that should be always-on, pair this with watchdog coverage so the "stayed down" case is caught within ~watchdog-tick-interval.
- **Mtime-based liveness probes fail for idle long-poll bots.** Bot.log is only written on startup + message events. An idle bot's log mtime is stuck at "last activity"; a dead bot's mtime is also stuck. Identical signals. For always-on launchd services, use `launchctl list <label>` PID extraction instead — non-zero PID = alive, no PID = dead. New `launchctl_label` signal_type added to watchdog this session.
- **Orphan-bot anti-pattern: `node bot.js` run manually outside launchd produces a process that ps sees but launchctl doesn't.** Symptom: `ps aux | grep bot.js` shows a PID; `launchctl list <label>` shows `-	0	<label>` (no PID). The orphan works until it dies, but launchd won't supervise/restart it. To restore launchd supervision: kill the orphan FIRST (otherwise `launchctl kickstart` would duplicate the bot and you'd get double Telegram polling), then `launchctl kickstart -k gui/$(id -u)/<label>`.
- **launchd plist `ProgramArguments` pointing at a Node script directly will NOT pick up `dotenv` config from script-relative .env.** Node's `import "dotenv/config"` reads from CWD, not __dirname. Solution: wrap in a small shell script that does `cd "$(dirname "$0")"` before `exec /opt/homebrew/bin/node bot.js`. Same lesson applies to any non-shell runtime under launchd that reads cwd-relative config files.
- **A `.job` file with inline `attempts=N` line beats sidecar files for retry tracking.** Single file = single thing to ls / mv to failed/ / parse. Same regex parser handles both fresh jobs (no attempts line, default 0) and retried jobs. Pattern: `grep -vE '^attempts=' "$job" > tmp && echo "attempts=$N" >> tmp && mv tmp "$job"`.
- **For multi-stage cron chains, propose timings from actual audit_log runtime evidence, not from reading scripts.** Curator's "looks fast in the source" turned out to be ~20 min wall-clock per the May 15 audit_log spread (`SELECT ts FROM audit_log WHERE actor_id='curator' ORDER BY ts`). Schedule pushed from operator's spec'd 07:30/08:15/08:30 to 07:00/07:15/08:15/08:45 to absorb that runtime. Future-CC: query `audit_log` for prior runs of each stage before proposing cron times.
- **Sister-session auto-commits will sweep up your untracked files under a generic subject.** I created `market/scribe/process_queue.sh` at ~23:21 PT; the 23:50 PT `agents-autocommit.sh` landed it as `3631871 agents: auto-commit 2026-05-23` — vague subject, no attribution to the actual feature. Future-CC: if you create a new file with intent to commit later, either commit it BEFORE 23:50 PT, OR reference the actual SHA in your own commit message when explaining what's in it (`3631871` in commit `23ccd52`).
- **`crontab` is user-scope macOS state, NOT tracked in any git repo.** Cron changes survive in `crontab -l` output only. Always back up before edit (`crontab -l > /tmp/crontab.before.<topic>`), diff after edit, and reference the new lines verbatim in the commit message that documents the activation — that's the only record of what was added.
- **Watchdog dry-run flagged a real bug (grammy-bot missing) within minutes of being deployed for that bot.** The `launchctl_label` probe correctly distinguished "launchd loaded but no PID" from "launchd not loaded". The orphan-bot case (process alive, launchd not tracking) shows as "missing" — which is exactly what you want, because if the orphan dies there's no auto-restart. Pattern: when adding watchdog coverage for a new service, run `--dry-run` immediately and treat any `missing` finding as a real signal, not noise.
- **The empty-day market brief shape (briefer's `[empty]` output → 289-byte placeholder to Telegram) is by design when no new videos publish that day.** Briefer queries today's briefs in `~/brain/channels/{cowen,casper}/briefs/` by DATE (publish date), not by file creation time. So manually-generated briefs for old videos won't trigger non-empty briefer output. Future-CC investigating "empty-day brief": confirm new video count for the date before assuming bug.

### 2026-05-24 — Verify render path, not raw URLs

When auditing internal link health, curling the URL inside a markdown link's parens and checking the HTTP status is NOT the verification. The site component
may rewrite the path at render time. On asbestoshq.com, `parseInlineLinks` in `GuideArticle.tsx` takes bare `/<slug>/` markdown links and rewrites to
`/guides/<slug>/` if the slug is in `APPROVED_GUIDE_SLUGS` (otherwise strips to plain text).

The first internal-link-fix pass (commit 36cb356) was based on raw-URL HTTP probes that returned 404. The JSON was actually correct as-written; the
render-time rewrite handled it. The "fix" prepended `/guides/` into the JSON, breaking the render-time match — `parseInlineLinks` saw `guides/<slug>` which
isn't in APPROVED_GUIDE_SLUGS, stripped 99 working links to plain text.

Rule: before "fixing" link formatting on a Next.js/React site, read the rendering code path. Specifically grep for the JSON field's render function and check
for any path-rewrite or whitelist-check logic. If the bare URL form is wrong-looking but the rendered output is correct, the JSON is right and the renderer
is doing work. Verify with `curl <page> | grep guide-inline-link` against the rendered HTML, not against the URL inside the markdown.

Corollary (also burned this session): when grepping rendered HTML for anchors, do NOT assume attribute order. The body links render as
`<a class="guide-inline-link" href="...">` (class-then-href); an href-then-class grep returns zero and looks like failure. Dump the raw tag before trusting a
count.

The damage was reverted in 711748b + 81ab725, and the corrected dead-target repoint shipped in c0b6471.
