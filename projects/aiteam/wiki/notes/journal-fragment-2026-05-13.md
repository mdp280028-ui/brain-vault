2026-05-13 late evening

spent way too much of today on triage instead of building. the distilled core and the build plan are tight now but the actual chassis isn't laid down yet, and tomorrow morning the temptation is going to be to read everything one more time instead of running `mkdir -p ~/brain` and starting.

decided: chassis goes down before I read anything else. CC executes top-to-bottom. if a step needs me to do something I do it, otherwise I let the model work.

also: stop comparing this to the friend bot in italy. that was scoped to feel good. this one is scoped to make $200/mo. different bar, different rules.
